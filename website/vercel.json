{
  "version": 2,
  "builds": [
    {
      "src": "build/*",
      "use": "@now/static"
    },
    {
      "src": "api/*.go",
      "use": "@now/go"
    }
  ],
  "routes": [
    { "src": "/api/items", "dest": "/api/items" },
    { "src": "/(.*)", "dest": "/build/$1" }
  ]
}
