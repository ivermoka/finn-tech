{
  "version": 2,
  "builds": [
    {
      "src": "api/*.go",
      "use": "@now/go"
    },
    {
      "src": "src/**/*",
      "use": "@now/static"
    }
  ],
  "routes": [
    { "src": "/api/items", "dest": "/api/items" },
    { "src": "/(.*)", "dest": "/src/$1" }
  ]
}
